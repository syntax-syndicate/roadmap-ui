{
  "name": "roadmap-ui",
  "version": "0.0.2",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "bump-deps": "npx npm-check-updates -u --deep && pnpm install",
    "package:build": "tsup",
    "package:dev": "tsup --watch",
    "prepublishOnly": "npm run build"
  },
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "devDependencies": {
    "@biomejs/biome": "1.9.3",
    "tsup": "^8.3.0",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3",
    "ultracite": "4.1.4"
  },
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=18"
  }
}
